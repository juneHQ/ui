
import { CartesianGrid } from 'recharts';

export const Grid = () => {
  debugger;
  return (
    <CartesianGrid
      className="stroke-1"
      strokeDasharray="3"
      vertical={false}
      horizontal={true}
      />
  );
}
